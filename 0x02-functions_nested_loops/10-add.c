#include <stdio.h>
#include "main.h"
/**
  * add -  two integers and returns the result.
  * @a: first integer
  * @b: second integer
  *
  * Return: result
  */
int add(int a, int b)
{
	return (a + b);
}
